services:
  postgres:
    image: 'postgres:17'
    environment:
      - 'POSTGRES_DB=selfblog'
      - 'POSTGRES_PASSWORD=123456'
      - 'POSTGRES_USER=root'
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: